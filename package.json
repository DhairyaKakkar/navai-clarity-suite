{
  "name": "navai",
  "version": "1.0.0",
  "private": true,
  "description": "NavAI monorepo — UI + Chrome extension",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@navai/ui",
    "build": "npm run build --workspace=@navai/ui && npm run build --workspace=@navai/extension",
    "dev:extension": "npm run watch --workspace=@navai/extension",
    "build:extension": "npm run build --workspace=@navai/extension",
    "pack:extension": "cd apps/extension && node build.mjs && cd dist && zip -r ../../extension.zip . && echo 'Created apps/extension.zip'",
    "lint": "npm run lint --workspace=@navai/ui",
    "typecheck": "npm run typecheck --workspace=@navai/extension",
    "test": "npm run test --workspace=@navai/ui",
    "clean": "rm -rf apps/ui/dist apps/extension/dist node_modules apps/ui/node_modules apps/extension/node_modules"
  }
}
