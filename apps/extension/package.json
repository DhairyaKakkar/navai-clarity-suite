{
  "name": "@navai/extension",
  "version": "1.0.0",
  "private": true,
  "description": "AI navigation assistant Chrome extension — guides you step-by-step through any website",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
